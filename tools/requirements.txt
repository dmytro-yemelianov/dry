# Dependencies for the independent Dry IR vector validator (tools/validate_vectors.py).
# Deliberately minimal and independent of dry-core: only a JSON Schema engine. The codec itself
# (DRY0/DRY1) is implemented from the Python standard library (zlib + struct + json).
jsonschema>=4.18
