# Dependencies for the independent Dry IR vector and proof-claim validators.
# Deliberately minimal and independent of dry-core: JSON Schema plus a TOML parser only on Python
# versions older than 3.11. The codec itself uses the standard library (zlib + struct + json).
jsonschema>=4.18
# `tomllib` entered the standard library in Python 3.11. The proof-claim validator keeps local
# Python 3.9/3.10 development usable without affecting newer interpreters.
tomli>=2.0; python_version < "3.11"
